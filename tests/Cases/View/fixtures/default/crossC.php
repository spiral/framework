<block:blockA>Block A defined in file cross C(default).</block:blockA>

<namespace:crossA>

    <block:blockM>
        <block:blockM/>
        Block M appended by file cross C(default).
    </block:blockM>

    <block:footer>
        <includes.blockB/>
    </block:footer>

</namespace:crossA>

<block:blockB>Block B defined in file cross C(default).</block:blockB>